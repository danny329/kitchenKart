<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grocery.daniel.kitchenkart">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/kkcolor"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".categoryMain"
            android:label="Kitchen Kart"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FruitVegCategoryActivity"
            android:label="Fruit and Vegetable"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BrandedFoodCategoryActivity"
            android:label="Branded Food"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GroceryStapleCategoryActivity"
            android:label="Grocery and Staple"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MeatCategoryActivity"
            android:label="Meat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DairyCategoryActivity"
            android:label="Diary"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BeveragesCategoryActivity"
            android:label="Beverage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:label="Registeration">
        </activity>
        <activity
            android:name=".checkoutcartActivity"
            android:label="Checkout" />
        <activity
            android:name=".OrderActivity"
            android:label="Order detail" />
        <activity
            android:name=".shipmentActivity"
            android:label="Shippment details">
        </activity>
        <activity
            android:name=".paymentActivity"
            android:label="Payment" />

        <activity android:name=".PurchaseActivity"
            android:label="purchased product" />

        <activity android:name=".CODActivity"
            android:label="Payment" />

        <activity android:name=".CardActivity"
            android:label="Payment" >
        </activity>

        <activity android:name=".AdminMainActivity"
            android:label="Admin " ></activity>

        <activity android:name=".ViewCustomerActivity"
            android:label="customer view" ></activity>

        <activity android:name=".InvoiceAllActivity"
            android:label="invoice view" />

        <activity android:name=".ProfileActivity"
            android:label="Profile" ></activity>

    </application>

</manifest>